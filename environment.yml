name: ML4Litho
channels:
  - defaults
dependencies:
  - zipfile
  - matplotlib
  - scikit-learn
  - numpy
  - pandas
  - tensorboard >= 1.11
  - tensorflow >= 1.12
  - pip:
    - beholder
    - jupyter-tensorboard
